## Playbooks & Scripts - Agent 工作框架说明

按照两层框架执行任务: **Playbooks (协调层) -> Scripts (执行层) **

- **Playbooks**: 存放在 `playbooks/` 的结构化流程文件。描述"应当发生什么"—包含默认执行路径、已知分叉的判断标准, 以及以「目的」为航标的即兴授权。
- **Scripts**: `scripts/` 下的确定性脚本 (.sh / .py), 负责文件操作、API 调用、数据转换等具体执行, 相同输入永远产生相同输出。

---

## 目录结构

```
项目根目录/
├── CLAUDE.md          # 本文件
├── playbooks/        # 协调层: Playbook 定义 (Markdown)
├── scripts/          # 执行层: 确定性执行脚本 (.sh/.py)
├── src/              # 项目源代码
├── tests/            # 测试文件
└── .tmp/             # 临时文件 (处理中间产物, 不提交)
```

---

## Playbook 编写规范

每个 Playbook 必须包含以下五个章节。

```markdown
# Playbook: [名称]

## 目的
[执行完成后, 发生了什么变化—用结果描述, 不用过程描述]

## 前提条件
- [文件、环境变量、依赖—缺失时在步骤开始前报错, 不进入步骤]

## 步骤
1. 【脚本调用】 `scripts/xxx` - 输入什么, 输出什么
2. 【判断】基于什么条件, 可能走向哪里

## 判断标准
- 遇到 [情况 A] 时, 选 X 而非 Y, 理由是 [...]
- (若本流程线性执行无分叉, 写明: "本流程线性执行, 无需预定义判断标准。")

## 验证
- [执行完成后, 如何确认结果符合预期]
```

「目的」是所有预置判断未覆盖情况时的兜底依据。「判断标准」把可预见的分叉提前声明, 未覆盖时回到「目的」自行决策。

---

## Script 编写规范

- **单一职责**: 一个 Script 只做一件事
- **确定性**: 相同输入必须产生相同输出, 不包含任何 AI 调用或随机逻辑
- **独立运行**: 可脱离 Playbook 单独在命令行执行和测试
- **清晰的输入输出**: 命令行参数接收输入, stdout 返回结果, exit code 表示成败

```bash
#!/bin/bash
# 用途: 检查当前项目缺失的依赖
# 参数: 无
# 输出: 逐行输出缺失的依赖包名称, 全部已安装则无输出
# 退出码: 0=执行完成, 1=执行出错
# Known Issues: (发现的边界情况记录在此)
```

**逻辑固定或会重复执行 -> 写成 Script; 需要理解上下文，做主观判断 -> 在 Playbook 中处理。**

---

## 纠错反馈循环

Script 执行失败时, 按以下五步完成修复:

1. **报告错误** -> 展示完整错误输出, 说明失败的 Script 和步骤
2. **分类错误** -> 判断属于以下哪一类 (分类决定修复位置):
   A. Script bug -> 修改 scripts/ 对应脚本
   B. 步骤设计有误 -> 更新 Playbook 的「步骤」章节
   C. 判断标准缺失 -> 补充 Playbook 的「判断标准」章节
   D. 环境/配置问题 -> 修复环境, 更新 Playbook 的「前提条件」章节
3. **修复** -> 按分类结果, 只改对应位置, 不做超出范围的改动
4. **重新验证** -> 再次运行, 确认输出符合预期
5. **更新记录** -> Script bug (类型 A): 将边界情况补充到 Script 头部的 Known Issues
   Playbook 变更 (类型 B/C/D): 已在步骤 2-3 中完成

修复后必须更新文档, 避免重蹈覆辙。临时文件放 `.tmp/`, 不强制覆盖未知文件。

---

## 执行规则

### 执行任务时

1. 查询 `playbooks/` 目录, 寻找匹配当前任务的 Playbook
2. 找到 Playbook 后, 先核查「前提条件」章节, 确认满足后再进入步骤
3. 执行步骤时, 遇到分叉先查「判断标准」章节; 未覆盖时参考「项目判断原则」; 两者都未覆盖时, 以 Playbook 的「目的」为准自行决策, 并说明推理
4. 没有匹配的 Playbook 时, 先判断是否值得创建新 Playbook, 再开始工作
5. 直接命令行调用 Script, 不在其他代码中模拟 Script 的行为
6. 优先复用已有 Script, 不重复造轮子

### 编写代码时

1. 不把判断逻辑写进 Script, Script 的内容应当是确定性的
2. 新建 Script 时遵循头部注释规范, 确保可独立运行
3. 新建 Playbook 时包含全部五个章节
4. 修改现有 Script 前, 先运行一次确认当前行为, 改完后再次运行验证
5. 单次实现量较大时, 先将任务拆分为可独立验证的子步骤, 逐段编写并验证后再继续

---

## 沟通风格

**所有输出和沟通必须使用中文**, 包括代码注释以外的所有文字。

1. 执行 Playbook 时, 每完成一个步骤简要报告结果, 再继续下一步
2. 遇到需要判断的环节, 说明推理过程和选择依据
3. Script 执行失败时, 先展示错误输出, 再按纠错反馈循环处理

---

## MGILT Website 项目进度

### 项目信息
- **项目路径**: `g:/claude code/MGILT_Website/`
- **主文件**: `index.html` (单文件网站，含所有 HTML/CSS/JS)
- **编辑器文件**: `js/editor-3d.js` (Three.js ES6 module)
- **本地服务器**: http://localhost:8806

### 已完成功能

#### 导航结构
- 左侧：搜索图标 + MGILT logo（居中对称）
- 中间：首页 | 模块系统 | 空间灵感 | 设计你的MGILT | 关于MGILT | 合作伙伴
- 右侧：用户图标

#### 编辑器 (设计你的MGILT)
- 标题：3D拼搭编辑器 → **MGILT 空间设计台**
- 已移除：橡皮擦按钮、填充按钮、对称按钮
- 新增"推荐组合"下拉菜单：
  - ☕ 咖啡店、🍽️ 餐厅外摆、💼 办公交流、🎓 学校庭院、🛋️ 休闲 lounge、🏪 零售店铺
- 填充/对称按钮：激活时变 sage 绿色 (#7A9E7E)
- 保存/更新功能：自定义模态框（居中显示）

#### 用户中心 (我的设计页面)
- 头像：默认显示用户名首字母，点击可上传自定义图片
- 用户名：点击可编辑，回车保存
- 设计卡片：显示截图预览或六边形模块图标

#### 设计管理
- 保存：弹出居中的模态框输入方案名称
- 更新：从"我的设计"点击编辑后，可点击"更新"按钮保存修改
- 删除：卡片右上角悬停显示 × 按钮，二次确认后删除

#### 登录系统
- 手机验证码登录（mock code: 1234）
- 手机快速注册
- localStorage 存储：session、users、designs

### 待修复/优化功能

#### 1. 头像上传功能（测试中）
- **问题**：点击头像后文件选择器无法弹出
- **位置**：`index.html` 第 1234 行
- **相关函数**：`handleDashAvatarChange`

#### 2. 更新功能（测试中）
- **问题**：`completeDesignLoad` 后 `pendingDesignLoad` 被设为 null
- **已修复**：`window.currentEditingDesignId` 保存设计 ID
- **相关函数**：`showUpdateModal`、`updateDesignToAccount`

### 下一步计划

1. **头像功能修复** - 确保文件选择器能正常弹出和上传
2. **用户名编辑优化** - 确保点击用户名能正常变成输入框并保存
3. **更新功能测试** - 确认从"我的设计"编辑后能正确更新
4. **清理调试代码** - 移除 console.log 调试语句
5. **浏览器兼容性测试** - 确保功能在各浏览器正常工作

### 技术备注

#### 关键函数暴露（ES6 模块）
由于 `editor-3d.js` 是 ES6 模块，需要将函数暴露到 window 对象才能在 HTML 中调用：
```javascript
window.navigateTo = navigateTo;  // 路由跳转
window.loadDesignToEditor = loadDesignToEditor;  // 加载设计到编辑器
window.showSaveModal = showSaveModal;  // 显示保存弹窗
window.showUpdateModal = showUpdateModal;  // 显示更新弹窗
window.doSaveDesign = doSaveDesign;  // 执行保存/更新
```

#### localStorage 数据结构
- `mgilt_session`: 当前用户会话
- `mgilt_users`: 所有注册用户
- `mgilt_designs`: 用户保存的设计方案（含 modules、screenshot）

---

## 超级技能（必须默认遵守）

### 技能 1：一键"拍快照"与"反悔"功能
**版本快照与安全回滚**

每当我（用户）要求你做一次**重大的、大面积的样式或功能修改**前，你**必须主动提示**我：

> "老板，这次改动较大，是否需要我先帮您把当前的代码备份一份？"

如果我说"要"或"备份"，请帮我在 `.tmp/` 文件夹下存一个当前文件的备份（命名格式如 `index.html.backup.20260331`），方便我随时"反悔"找回原来的效果。

---

### 技能 2：视觉驱动与组件化思维
**现代审美与模块化拆分**

规则：
- 写 CSS 样式时，**默认使用现代、简洁、大气的设计语言**（如大留白、精致的阴影、平滑的圆角）
- 每次写新功能，尽量把 HTML 和 CSS 拆分成**独立的、像乐高积木一样的"小组件"**
- 比如独立的导航栏组件、独立的卡片组件、独立的按钮组件等
- 这样方便我随时挪动位置、复用或替换

---

### 技能 3：自动写"人话注释"
**保姆级"人话"代码注释**

规则：你在帮我写 HTML/CSS/JS 代码时，**必须在关键地方加上极度通俗的中文注释**。不要用专业术语，要用小白能听懂的话。

错误示范：
```javascript
// 绑定 click 事件，触发 fetch 异步请求
```

正确示范：
```javascript
// 当用户点击这个按钮时，去网络上抓取最新的数据显示在屏幕上
```
